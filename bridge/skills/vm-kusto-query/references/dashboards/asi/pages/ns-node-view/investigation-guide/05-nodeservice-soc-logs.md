# NodeService SoC Logs

> Source: **NodeService - NodeService_NodeView** dashboard, chapter **NodeService SoC Logs** (1 queries across 1 sub-groups).

Each KQL block is preserved verbatim from the dashboard. Substitute params (`{globalFrom}`, `{nodeId}`, etc.) with case values, then execute via vm-kusto-query / kusto_runner.py / replay.py.

---

## (no subgroup)

### Echo

_Widget purpose:_ NodeService SoC Logs

Cluster: `?` · Database: `?` · Type: `Single` · Widget: `Markdown`
Source panel: `NodeService SoC Logs`

```kusto
return [data];
```

**Params:** `{_nodeId}`, `{_timeOfFault}`

---
